export function MultipleObjectsView() {
  return (
    <div className="flex flex-col gap-4 p-4">
      <h1 className="text-3xl font-bold">Level 5: Multiple Objects</h1>
      <p className="text-muted-foreground">Create an array of objects (each with x, y, radius, color) and loop through them to draw multiple moving circles.</p>
      <div className="mt-8 rounded-lg border-2 border-dashed border-muted p-12">
        <p className="text-center text-muted-foreground">Your implementation goes here</p>
      </div>
    </div>
  );
}
